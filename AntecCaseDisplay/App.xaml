<Application x:Class="AntecCaseDisplay.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="clr-namespace:H.NotifyIcon;assembly=H.NotifyIcon.Wpf"
             xmlns:local="clr-namespace:AntecCaseDisplay"
             ShutdownMode="OnExplicitShutdown"
             Startup="OnStartup"
             Exit="OnExit">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Light.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Theme only the window chrome and plain text readouts. Input
                 controls (TextBox, ComboBox, Button) and tray MenuItems keep
                 their system-default look so the OS-drawn backgrounds don't
                 fight with the Foreground we'd otherwise force — that was
                 producing white-on-white text in the ComboBox display area
                 and the tray context menu. -->

            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            </Style>

            <Style TargetType="Label">
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                <Setter Property="Margin" Value="0,3"/>
            </Style>

            <Style TargetType="Button">
                <Setter Property="Padding" Value="14,6"/>
                <Setter Property="MinWidth" Value="90"/>
                <Setter Property="Margin" Value="6,0,0,0"/>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Padding" Value="4,3"/>
            </Style>

            <Style TargetType="GroupBox">
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="Margin" Value="0,0,0,12"/>
            </Style>

            <!-- tray icon resource -->
            <tb:TaskbarIcon x:Key="TrayIcon"
                            x:Shared="True"
                            ToolTipText="AntecCaseDisplay"
                            Visibility="Visible"
                            NoLeftClickDelay="True"
                            LeftClickCommand="{x:Static local:AppCommands.OpenSettingsCommand}">
                <tb:TaskbarIcon.IconSource>
                    <tb:GeneratedIconSource Text="A"
                                            Foreground="White"
                                            Background="#FF0078D4"
                                            FontWeight="Bold"
                                            FontSize="24"/>
                </tb:TaskbarIcon.IconSource>
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Open settings…"
                                  Command="{x:Static local:AppCommands.OpenSettingsCommand}"
                                  FontWeight="Bold"/>
                        <MenuItem Header="Pause / Resume"
                                  Command="{x:Static local:AppCommands.PauseResumeCommand}"/>
                        <Separator/>
                        <MenuItem Header="Quit"
                                  Command="{x:Static local:AppCommands.QuitCommand}"/>
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
</Application>
